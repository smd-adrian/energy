export interface RenewableProductionItem {
  region: string;
  fuenteEnergia: string;
  produccionTotal: number;
  unidad: string;
}

export interface TopWindCountryItem {
  pais: string;
  produccionTotal: number;
  unidad: string;
}
